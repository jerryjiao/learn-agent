# Easy Dataset 部署指南

---

## 项目简介

**Easy Dataset** 是一个专为创建大型语言模型（LLM）微调数据集而设计的强大工具。它提供了直观的界面，用于：

- 📄 上传和处理多种格式的文档（PDF、Markdown、DOCX 等）
- ✂️ 智能文本分割和可视化分段
- ❓ 自动生成问题
- 🏷️ 智能构建领域标签
- 💬 使用 LLM API 生成答案
- 📦 导出多种格式的数据集（Alpaca、ShareGPT 等）

本部署指南将帮助您使用 Docker Compose 快速部署 Easy Dataset 服务。

---

## 前置要求

在开始部署之前，请确保您的系统满足以下要求：

### 1. 系统要求

- **操作系统**：Linux、macOS 或 Windows（推荐 Linux）
- **内存**：至少 8GB RAM
- **磁盘空间**：至少 20GB 可用空间
- **网络**：能够访问 GitHub Container Registry

### 2. 软件依赖

#### 验证依赖

```bash
# 检查 Docker 版本
docker --version

# 检查 Docker Compose 版本
docker compose version

```

---

## 快速开始

如果您已经熟悉 Docker，可以快速启动服务：

```bash
# 1. 进入部署目录
cd 05-agent-model-finetuning/easy-dataset/deploy

# 2. 启动服务（后台运行）
docker compose up -d

# 3. 查看服务状态
docker compose ps

# 4. 查看日志
docker compose logs -f

# 5. 访问应用
# 打开浏览器访问：http://localhost:1717
```

---


## 配置说明

### 端口配置

默认端口为 `1717`。如果需要修改端口，编辑 `docker-compose.yml`：

```yaml
ports:
  - '8080:1717'  # 将宿主机端口改为 8080
```

修改后需要重启服务：

```bash
docker compose down
docker compose up -d
```

### 数据持久化

数据存储在以下位置：

- **数据库文件**：`./local-db/` 目录

**重要提示**：
- 这些目录中的数据会持久化保存
- 删除容器不会删除数据
- 建议定期备份这些目录

### 镜像版本

当前使用的镜像版本为 `1.6.1`。如需使用其他版本：

```yaml
image: ghcr.io/conardli/easy-dataset:latest  # 使用最新版本
# 或
image: ghcr.io/conardli/easy-dataset:1.5.0   # 使用特定版本
```

查看可用版本：
- [GitHub Releases](https://github.com/ConardLi/easy-dataset/releases)
- [Docker Image](https://github.com/conardli/easy-dataset/pkgs/container/easy-dataset)

---
