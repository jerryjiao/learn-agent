# ============================================================================
# AI旅行规划智能体 - 环境变量配置文件
# ============================================================================
#
# 这个文件定义了系统运行所需的所有环境变量和API密钥。
# 环境变量是一种安全的方式来存储敏感信息（如API密钥），
# 避免将这些信息直接写在代码中。
#
# 适用于大模型技术初级用户：
# - 环境变量是现代软件开发的标准实践
# - 它们允许在不修改代码的情况下配置应用程序
# - 这种方式提高了安全性和部署的灵活性
# ============================================================================

# ----------------------------------------------------------------------------
# 🚀 核心API配置 (AI旅行规划智能体必需)
# ----------------------------------------------------------------------------

# OpenAI 兼容 API 密钥 (必需 - AI旅行规划智能体的核心)
# 功能说明：
# - 支持官方 OpenAI 及国内常见的 OpenAI 兼容网关（如 DeepSeek、通义千问等）
# - 用于驱动所有 AI 智能体的推理和决策
# - 支持通过自建代理实现国内可访问的模型调用
# - 获取方式：根据所选服务商在控制台创建密钥
OPENAI_API_KEY=XXXXX

# OpenAI 兼容接口基础地址 (可选)
# 功能说明：
# - 自定义 OpenAI 风格接口的 Base URL
# - 例如使用 DeepSeek 可填写：https://api.deepseek.com/v1
# - 若使用官方 OpenAI，可保留默认值 https://api.openai.com/v1
OPENAI_BASE_URL=https://api.apiyi.com/v1

# OpenAI 兼容模型名称 (可选)
# 功能说明：
# - 指定默认调用的模型名称
# - 可根据服务商提供的模型（如 deepseek-chat、qwen-max、gpt-4o-mini 等）进行调整
OPENAI_MODEL=deepseek-chat

# ----------------------------------------------------------------------------
# 🔧 传统系统API配置 
# ----------------------------------------------------------------------------

# 和风天气 (QWeather) API配置 (可选)
# 功能说明：
# - 提供国内稳定可访问的实时天气、灾害预警、多日预报
# - 本项目默认使用和风天气替换 OpenWeather
# - 开发者平台：https://dev.qweather.com/
QWEATHER_API_BASE=XXXXX
QWEATHER_API_KEY=XXXXX


